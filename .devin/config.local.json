{
  "permissions": {
    "allow": [
      "Exec(npx)",
      "Exec(mv)",
      "Exec(rmdir ufguessr-temp)",
      "Exec(git status)",
      "Exec(git log)",
      "Exec(git branch)",
      "Exec(npm install)"
    ]
  }
}